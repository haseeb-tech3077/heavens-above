{
  "name": "heavens-above",
  "version": "1.0.0",
  "description": "Astronomical data web application",
  "scripts": {
    "test": "echo 'No tests' && exit 0",
    "lint": "echo 'No linting' && exit 0",
    "build": "echo 'No build' && exit 0"
  },
  "devDependencies": {},
  "dependencies": {}
}